/** @type {import('tailwindcss').Config} */
export default {
  content: ["./src/**/*.{html,js,jsx}"],
  darkMode: "class", // vagy 'media' az automatikus váltáshoz az operációs rendszer beállításai alapján
  theme: {
    extend: {},
  },
  plugins: [],
};
